version: '3.8'

services:
  database:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tutorial
    mem_limit: 512m
    cpus: 1
    ports:
      - '${DB_PORT:-3535}:3306'
    volumes:
      - ./database:/var/lib/mysql
volumes:
  database:
